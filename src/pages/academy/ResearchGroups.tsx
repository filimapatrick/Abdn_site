import React from 'react';
import { motion } from 'framer-motion';
import { Users, Brain, Dna, Network } from 'lucide-react';

export default function ResearchGroups() {
  return (
    <main className="pt-20">
      {/* Hero Section */}
      <section className="relative py-20 bg-gradient-to-br from-amber-800 to-amber-600">
        {/* ... rest of the section */}
      </section>

      {/* Research Groups Section */}
      <section className="py-24 bg-amber-50">
        {/* ... rest of the section */}
      </section>

      {/* Benefits Section */}
      <section className="py-24 bg-amber-100/50">
        {/* ... rest of the section */}
      </section>
    </main>
  );
}
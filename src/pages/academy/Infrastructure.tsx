import React from 'react';
import { motion } from 'framer-motion';
import { Database, Server, Microscope, Beaker } from 'lucide-react';

export default function Infrastructure() {
  return (
    <main className="pt-20">
      {/* Hero Section */}
      <section className="relative py-20 bg-gradient-to-br from-amber-900 to-amber-700">
        {/* ... rest of the section */}
      </section>

      {/* Facilities Section */}
      <section className="py-24 bg-amber-50">
        {/* ... rest of the section */}
      </section>

      {/* Features Grid */}
      <section className="py-24 bg-amber-100/50">
        {/* ... rest of the section */}
      </section>
    </main>
  );
}